# Family modules are resolved lazily via hard_problems.core.get_family(name).
# Do not import family modules here: modules must stay independently loadable
# (and independently developable) per docs/ARCHITECTURE.md section 4.
